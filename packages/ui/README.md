# packages/ui

Shared components used by both `apps/mobile` and `apps/admin` where their
design needs actually overlap (e.g. badges, filter chips). Not populated
yet — created empty so component extraction has a home once Phase 2/3 UI
work starts, instead of components getting stranded inside one app first.
