# packages/types

Shared TypeScript types: database row types (generated from
`/supabase/migrations` via the Supabase CLI once the project is linked)
and API request/response contracts shared between `apps/mobile` and
`apps/admin`. Not populated yet — depends on a live Supabase project to
generate from.
