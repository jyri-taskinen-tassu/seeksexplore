# packages/api

Typed Supabase client plus query/mutation hooks (TanStack Query) shared
between `apps/mobile` and `apps/admin` — e.g. `useRoutes(destinationId,
filters)`, `useNearbyPois(point, radiusM)`. Not populated yet; depends on
`packages/types` (generated DB types) and a live Supabase project.
