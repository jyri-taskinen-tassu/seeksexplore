# packages/database

Generated Supabase types (`supabase gen types typescript`) and low-level
query helpers built directly on the schema in `/supabase/migrations`. Not
populated yet — depends on a linked Supabase project.
