# packages/config

Shared ESLint, TypeScript, and Tailwind/NativeWind config used by both
`apps/mobile` and `apps/admin`, plus the centralized error-handling/logging
helper referenced in `../../docs/architecture.md` §13. Not populated yet —
added once both apps exist and the duplication is real, not speculative.
