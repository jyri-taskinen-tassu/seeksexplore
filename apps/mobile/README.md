# apps/mobile — Destination Outdoor App

Not scaffolded yet. This directory is reserved for the Expo / React Native
consumer app (Phase 1 continuation → Phase 2 "Map").

Planned stack: Expo + TypeScript + Expo Router, TanStack Query, Zustand,
NativeWind. See `../../docs/architecture.md` for the full picture and
`../../docs/database-schema.md` for what the app reads/writes.

Per project scope, UI work here starts only after the database schema and
domain architecture have landed (`/supabase/migrations`, `/docs`) — which
this commit provides.
