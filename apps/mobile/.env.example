# Copy to .env.local and fill in once a Supabase project exists (see /docs).
# EXPO_PUBLIC_* vars are bundled into the client — never put service-role or
# other secret keys here, only the public URL and anon key.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
