# apps/admin — Platform Admin Dashboard

Not scaffolded yet. Reserved for the Next.js + TypeScript admin dashboard
(Phase 5): destination management, routes, POIs, businesses, products,
sponsored placements, analytics.

This is a separate Next.js app from the existing landing page at the repo
root — they do not share code or deployment config. See
`../../docs/architecture.md` §6 (roles) for the access model
(`super_admin` / `destination_admin` / `business_admin`).
